import "yu/prelude.yu"
import "yu/Ordering?.yu"


val.. _.quicksort\List[A : Ty] : List A & Ordering? A -> List A
let nil _ => nil
let (x :: xs) cmp =>
  xs .filter (cmp .lt? _ x) .quicksort cmp
  :+: (x :: nil)
  :+: xs .filter (cmp .ge? _ x) .quicksort cmp
